# Database (required for backend)
DATABASE_URL="postgresql://ims:ims_secret@localhost:5432/ims_db"

# JWT for auth (optional; defaults to dev-secret)
JWT_SECRET="your-secret-key-change-in-production"
JWT_EXPIRES_IN="7d"

# Server port (optional; default 3000)
PORT=3000
